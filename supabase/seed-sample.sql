-- =============================================================================
-- ข้อมูลสมมติสำหรับ local และ preview เท่านั้น — ห้ามรันกับ Production
--
-- ไฟล์นี้มีหน่วยงานและตำแหน่งตัวอย่าง เพื่อให้เปิดระบบขึ้นมาแล้วมีข้อมูลให้เลือก
-- ทันทีตอนพัฒนาและตอนสาธิต ทุกชื่อกำกับด้วยคำว่า "(ตัวอย่าง)" เพื่อให้เห็นชัด
-- ว่าไม่ใช่ข้อมูลจริง หากหลุดไปอยู่ในระบบจริงจะสังเกตได้ทันที
--
-- สำหรับ Production ให้ข้ามไฟล์นี้ แล้วบันทึกหน่วยงานและตำแหน่งจริงของโรงเรียน
-- ผ่านหน้าจัดการข้อมูลพื้นฐาน (Phase 2) หรือด้วย SQL ที่เขียนขึ้นเฉพาะ
--
-- ต้องรัน seed-reference.sql ก่อนเสมอ
--
-- ข้อห้าม (แผนข้อ 14.2, 17.1):
--   * ห้ามใส่ชื่อโรงเรียนจริงหรือชื่อบุคคลจริง
--   * ห้ามรันไฟล์นี้กับฐานข้อมูล Production
-- =============================================================================

-- -----------------------------------------------------------------------------
-- หน่วยงานและตำแหน่งสมมติสำหรับ dev/preview
-- ชื่อทั้งหมดเป็นชื่อทั่วไป ไม่อ้างอิงโรงเรียนหรือบุคคลจริง
-- -----------------------------------------------------------------------------

insert into public.departments (code, name_th) values
  ('ADMIN',    'กลุ่มบริหารงานทั่วไป (ตัวอย่าง)'),
  ('ACADEMIC', 'กลุ่มบริหารวิชาการ (ตัวอย่าง)'),
  ('BUDGET',   'กลุ่มบริหารงบประมาณ (ตัวอย่าง)')
on conflict (code) do nothing;

insert into public.positions (code, name_th, is_signatory) values
  ('DIRECTOR',   'ผู้อำนวยการ (ตัวอย่าง)',        true),
  ('DEPUTY',     'รองผู้อำนวยการ (ตัวอย่าง)',      true),
  ('OFFICER',    'เจ้าหน้าที่พัสดุ (ตัวอย่าง)',    false),
  ('TEACHER',    'ครู (ตัวอย่าง)',                false)
on conflict (code) do nothing;

-- -----------------------------------------------------------------------------
-- ข้อมูลพื้นฐานสมมติสำหรับ Phase 2 (หมวดพัสดุ แหล่งเงิน สถานที่)
--
-- หน่วยนับไม่ได้อยู่ที่นี่ เพราะ "ชิ้น" "กล่อง" "รีม" เป็นหน่วยสากลที่ใช้จริง
-- ทุกโรงเรียน ไม่ใช่ข้อมูลสมมติ จึงอยู่ใน seed-reference.sql
--
-- ปีงบประมาณและข้อมูลโรงเรียนจงใจไม่ใส่ไว้ที่นี่ เพราะเป็นค่าที่โรงเรียน
-- ต้องกรอกเองให้ตรงกับความจริง (คำถาม Q6 และ Q7 ใน docs/assumptions.md)
-- การใส่ค่าสมมติไว้เสี่ยงให้มีคนเผลอใช้ต่อโดยไม่แก้
-- -----------------------------------------------------------------------------

insert into public.funding_sources (code, name_th) values
  ('BUDGET',  'เงินงบประมาณ (ตัวอย่าง)'),
  ('INCOME',  'เงินรายได้สถานศึกษา (ตัวอย่าง)'),
  ('SUBSIDY', 'เงินอุดหนุน (ตัวอย่าง)')
on conflict (code) do nothing;

insert into public.item_categories (code, name_th, kind) values
  ('SUP-OFFICE',  'วัสดุสำนักงาน (ตัวอย่าง)',   'SUPPLY'),
  ('SUP-CLEAN',   'วัสดุงานบ้านงานครัว (ตัวอย่าง)', 'SUPPLY'),
  ('AST-COMPUTER', 'ครุภัณฑ์คอมพิวเตอร์ (ตัวอย่าง)', 'ASSET'),
  ('AST-FURNITURE', 'ครุภัณฑ์สำนักงาน (ตัวอย่าง)', 'ASSET')
on conflict (code) do nothing;

insert into public.locations (code, name_th) values
  ('STORE-01', 'ห้องพัสดุกลาง (ตัวอย่าง)'),
  ('OFFICE-01', 'ห้องธุรการ (ตัวอย่าง)')
on conflict (code) do nothing;

-- -----------------------------------------------------------------------------
-- บัญชีงบประมาณจงใจไม่ใส่ไว้ที่นี่
--
-- บัญชีงบต้องผูกกับปีงบประมาณและโครงการ ซึ่งทั้งสองอย่างเป็นค่าที่โรงเรียน
-- ต้องกรอกเองให้ตรงกับความจริง (คำถาม Q6 ใน docs/assumptions.md)
-- การใส่ปีงบสมมติเพื่อให้ seed บัญชีงบได้ จะทำให้มีคนเผลอใช้ต่อโดยไม่แก้
-- ซึ่งอันตรายกว่าการไม่มีข้อมูลตัวอย่างเลย
--
-- การทดสอบ budget ledger สร้าง fixture ของตัวเองในทรานแซกชันที่ rollback
-- จึงไม่ต้องพึ่ง seed (ดู supabase/tests/run-budget-tests.sh)
-- -----------------------------------------------------------------------------
